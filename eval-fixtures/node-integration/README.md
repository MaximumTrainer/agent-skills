# athlete-sync

Pulls wellness and activity data from the provider API and derives dashboard state.

## Known API behaviours

See `docs/API-QUIRKS.md`.

## Testing

`npm run verify` builds and runs the suite. Fixtures live in `tests/fixtures/`.
