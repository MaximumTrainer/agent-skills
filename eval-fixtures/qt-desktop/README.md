# TrainerView

Qt6 desktop client. Migrated from Qt5 in 2026-02.

## Build

```bash
cmake -S . -B build && cmake --build build
```

## Packaging

`packaging/build-appimage.sh` produces an AppImage. Tested on the build machine.
