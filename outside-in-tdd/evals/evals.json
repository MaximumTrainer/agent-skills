{
  "skill_name": "outside-in-tdd",
  "evals": [
    {
      "id": 1,
      "prompt": "Can you implement issue #42 for me? It's 'reject duplicate flow names with a 409'. The repo is a Kotlin/Spring service, hexagonal layout, and the team is strict about TDD.",
      "expected_output": "Reads the issue, works outside-in from the acceptance criterion, commits a failing test before the implementation, and confirms each test failed for the right reason before moving inward.",
      "files": [],
      "expectations": [
        "The response reads the issue before writing any code",
        "The response names which ring it is entering at and why",
        "A failing test is written and run BEFORE any production code",
        "The response explicitly confirms the test failed for the right reason (missing behaviour, not a typo or harness error)",
        "The red test is committed as its own commit, separate from the implementation",
        "Commit messages carry the issue reference (#42)",
        "The response does not use --no-verify at any point",
        "An in-memory implementation of the port is preferred over a mocking framework for the repository"
      ],
      "discriminating": [
        3,
        4
      ]
    },
    {
      "id": 2,
      "prompt": "here's my PR, can you review whether it was actually done test-first? git log --oneline shows:\na1b2c3d test: add tests for flow uniqueness\ne4f5g6h feat: add flow name uniqueness check\n(that's the whole branch, newest first)",
      "expected_output": "Reads the log newest-first, so the implementation commit is the older one and the tests were committed after it. Says the history shows test-after, and that ordering alone is weak evidence either way - the test must be shown to fail without the implementation.",
      "files": [],
      "expectations": [
        "The response reads git log as newest-first and identifies the feat commit as the older one",
        "The response states the tests were committed after the implementation",
        "The response does not approve the change as test-driven on the strength of the commit order",
        "The response proposes checking out the test commit and running it to see whether it fails",
        "The response notes that commit ordering alone can be manufactured by staging or rebasing"
      ],
      "discriminating": [
        3,
        4
      ]
    },
    {
      "id": 3,
      "prompt": "We shipped a bug: our billing service charged a customer twice because a retry re-sent the same payment request. I've found the cause - the idempotency key was being regenerated per attempt instead of once per payment. Can you fix it?",
      "expected_output": "Identifies that the change is a rule inside existing plumbing, so the entry ring is a unit test over a pure function, and pushes the clock and randomness out as injected dependencies so the test is deterministic.",
      "files": [],
      "expectations": [
        "The response writes a failing test that reproduces the double charge before changing the code",
        "The response identifies which test ring should have caught this and did not",
        "The response treats the absent test ring as a finding worth reporting, not just an implementation detail",
        "The response states the observed failure of the reproduction test, not merely that it was run",
        "The response does not weaken or skip an existing test to get to green"
      ],
      "discriminating": [
        1,
        2,
        3
      ]
    }
  ]
}
